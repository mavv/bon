import React, { Component } from 'react'
import ReactDOM from 'react-dom'

class HooksTryouts extends Component {
    render() {
        return <div>Hook hey hey!</div>
    }
}

ReactDOM.render(<HooksTryouts />, document.getElementById('react-hooks'))
